As soon as we clean up the [[https://github.com/MBcode/ec/blob/master/system.md][system]] connections, (by removing the un-needed cache &it's hard coded naming pratices) we can use this below again as well, to [[get.md][get]] the output of a crawl and use it

crawlLD extruct js/onLD, then checkSite on sitemap updates and ld2nt conversion
[[flow.svg]]
More on ETL for annotaions soon
[[etl.svg]]
